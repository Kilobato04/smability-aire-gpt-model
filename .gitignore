cat > .gitignore <<EOF
# --- Python & Sistema ---
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
.DS_Store

# --- Credenciales y Seguridad (CRÍTICO) ---
.env
aws_config.txt

# --- Datos Pesados (Data Lake) ---
# Los datos viven en S3, nunca en el repo
*.tif
*.zip
*.csv
/data/
/training/raw_data/
/tmp/

# --- Artefactos Generados (Modelos y Logs) ---
# Ignoramos los modelos para no ensuciar el repo (se guardan en S3)
model_*.json
models/
response.json
response_live.json
out.json
out_f.json

# --- AWS/CloudShell ---
.aws/
heavy_source.zip
EOF
