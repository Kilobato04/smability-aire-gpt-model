version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo "📦 Instalando dependencias (FFmpeg, Playwright, Requests)..."
      - apt-get update -y
      - apt-get install -y ffmpeg
      - pip install playwright boto3 requests
      - playwright install --with-deps chromium
  build:
    commands:
      - if [ -z "$FLOW_ID" ]; then echo "❌ ERROR FATAL - FLOW_ID esta vacio. Abortando fabrica."; exit 1; fi
      - echo "🎬 Generando Reel y publicando en Instagram..."
      - cd marketing_reels
      - python render_reel.py
  post_build:
    commands:
      - echo "✅ Misión cumplida. El script de Python ya subió el video a S3 y a Meta."
      - echo "🧹 Limpieza automática completada. Cero archivos duplicados."
