version: 0.2

env:
  variables:
    ECR_REPO_NAME: "smability-chatbot"
    LAMBDA_FUNC_NAME: "Smability-Chatbot"
    AWS_REGION: "us-east-1"

phases:
  pre_build:
    commands:
      - echo "🤖 Iniciando Build del Bot..."
      # --- LA CORRECCIÓN MÁGICA ---
      - echo "🆔 Obteniendo Account ID..."
      - export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      # ----------------------------
      - echo "🔑 Logueando en ECR ($AWS_ACCOUNT_ID)..."
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com
      - REPOSITORY_URI=$AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$ECR_REPO_NAME
      - IMAGE_TAG=latest

  build:
    commands:
      - echo "📂 Entrando a carpeta del Bot..."
      - cd app/airegpt_telegram
      - echo "🐳 Construyendo Docker..."
      - docker build -t $REPOSITORY_URI:$IMAGE_TAG .

  post_build:
    commands:
      - echo "⬆️ Subiendo a ECR..."
      - docker push $REPOSITORY_URI:$IMAGE_TAG
      - echo "🔄 Actualizando Lambda..."
      - aws lambda update-function-code --function-name $LAMBDA_FUNC_NAME --image-uri $REPOSITORY_URI:$IMAGE_TAG
      - echo "✅ Despliegue completado."
