version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "📦 Instalando librerías..."
      - pip install pandas xgboost scikit-learn boto3 numpy requests

  pre_build:
    commands:
      - echo "⬇️ Descargando Dataset..."
      - aws s3 cp s3://smability-data-lake/raw_data/dataset_aire_zmcdmx.zip /tmp/dataset.zip
      - unzip /tmp/dataset.zip -d /tmp/dataset_final

  build:
    commands:
      - echo "🧠 INICIANDO ENTRENAMIENTO GRAND SLAM (5 Contaminantes)..."
      # AJUSTE: Ejecutamos el script apuntando a la carpeta training/
      - python training/train_v5_grand_slam.py

  post_build:
    commands:
      - echo "✅ Entrenamiento finalizado."
